__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store
.pytest_cache/

# Streamlit secrets — never commit. Configure in Streamlit Cloud's
# deploy settings UI for production; create locally for dev only.
.streamlit/secrets.toml

# Don't commit real customer data - sample CSVs only
*.csv
!tests/sample_orders_export.csv

# Suraya brain: per-project local jsonl emitted by /capture skill
.observations.jsonl
